import InteractiveTourCustomizer from "@/components/InteractiveTourCustomizer";

export const metadata = {
  title: "Ceylon Tour Customizer - Interactive Route Builder",
  description: "Plan your trip through Sri Lanka. Pick destinations on an interactive map, configure nights, add-ons, and request booking slot confirmation.",
};

export default function CustomizeTourPage() {
  return <InteractiveTourCustomizer />;
}
